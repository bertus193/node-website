<% include header.ejs %>

<body>
<div class="container">
   <div class="jumbotron text-center">
            <div id="yoquese"></div>
      
      <script type="text/babel">
         var test = React.createClass({
         
          
        testing(url) {
          fetch(url)
    .then(function(res) {
        return res.text();
    }).then(function(body) {
        document.getElementById("lol").innerHTML = body;
    });
        },
        
         render() {
            return (
                  <div>
                  <div id="lol"></div>
                  <div>
                    Prueba
                    <button className="btn btn-primary" 
                                      onClick={this.testing.bind(this,'/test2')}>Prueba</button>
                 </div>
                 </div>
            );
         }
         })
         //mostramos el componente
         ReactDOM.render(
            React.createElement(test),
            document.getElementById('yoquese')
         )
      </script>
   </div>
</div>
</body>
</html>